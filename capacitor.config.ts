import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.pruebapush',
  appName: 'pushNotif',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
